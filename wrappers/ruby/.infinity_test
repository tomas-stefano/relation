#!/usr/bin/env ruby
infinity_test do
  
  before :all do
    system('rake compile')
  end

  use :test_framework => :rspec, :rubies => %w(1.9.2)

  heuristics do
    add("^ext/*/(.*)\.c") do |file|
      run :all => :tests
    end
  end
end
